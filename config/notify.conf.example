# Notification configuration for Agent Box
#
# Copy this to /data/config/notify.conf and customize

# ntfy server (default: https://ntfy.sh)
# You can self-host ntfy or use the public server
NTFY_SERVER="https://ntfy.sh"

# Your ntfy topic - make this unique and hard to guess
# Anyone who knows this topic can send you notifications
NTFY_TOPIC="agent-box-CHANGEME"

# Default priority: min, low, default, high, urgent
NTFY_PRIORITY="default"
